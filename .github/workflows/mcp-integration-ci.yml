name: MCP Integration CI

on:
  pull_request:
    branches: [main]
    paths:
      - "apps/web/src/lib/mcp/**"
      - "apps/web/src/db/mcp-schema.ts"
      - "apps/web/src/app/api/mcp/**"
      - "apps/web/src/app/.well-known/**"
      - "apps/web/src/app/oauth/**"
      - "apps/web/drizzle/0037_mcp_oauth_foundation.sql"
      - "apps/web/drizzle/0038_mcp_read_only_rls.sql"
      - "apps/web/drizzle/0039_mcp_grant_permission_consistency.sql"
      - "apps/web/drizzle/0040_mcp_distributed_rate_limit.sql"
      - "apps/web/drizzle/meta/_journal.json"
      - "apps/web/drizzle.config.ts"
      - "apps/web/eslint.config.mjs"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/mcp-integration-ci.yml"
  push:
    branches:
      - "feat/mcp-oauth-integration"

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install exact dependencies
        run: npm ci --no-audit --no-fund

      - name: Audit production dependencies
        run: npm audit --omit=dev --audit-level=high

      - name: Typecheck
        run: npm run typecheck

      - name: Lint MCP integration files
        run: >-
          npx eslint
          apps/web/src/lib/mcp
          apps/web/src/db/mcp-schema.ts
          apps/web/src/app/api/mcp
          apps/web/src/app/.well-known

      - name: Unit and migration contract tests
        run: npm test -- --run src/lib/mcp

      - name: Build production application
        env:
          DATABASE_URL: "postgresql://postgres:postgres@127.0.0.1:5432/postgres"
          MCP_ENABLED: "false"
          MCP_WRITES_ENABLED: "false"
          NEXT_PUBLIC_SUPABASE_URL: "https://example.supabase.co"
          NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: "sb_publishable_ci_placeholder"
          NEXT_PUBLIC_SITE_URL: "https://example.com"
        run: npm run build
