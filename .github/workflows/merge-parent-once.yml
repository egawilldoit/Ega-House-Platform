name: Merge Parent Once

on:
  push:
    branches:
      - arch/08-native-project-goal-ui
    paths:
      - .github/workflows/merge-parent-once.yml

permissions:
  contents: write

jobs:
  merge:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
        with:
          ref: arch/08-native-project-goal-ui
          fetch-depth: 0
      - name: Merge corrected stage 7 parent
        shell: bash
        run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git fetch origin arch/07-web-app-workspace:refs/remotes/origin/arch/07-web-app-workspace
          git merge --no-ff --no-edit origin/arch/07-web-app-workspace
          git push origin HEAD:arch/08-native-project-goal-ui
