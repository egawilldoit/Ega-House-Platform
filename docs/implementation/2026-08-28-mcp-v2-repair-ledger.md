# Repair Ledger — MCPv2 2026-07-28 — PR #183 @ 242185d → repair

**Branch:** `feat/mcp-v2-full-read-write` **Worktree:** `/home/ubuntu/ega-house/.worktrees/mcp-v2-full-read-write`
**PR:** https://github.com/egawilldoit/Ega-House-Platform/pull/183
**Base:** `1a01bf3d03bf2394358f204448d247f1b04d544e` **Head audited:** `242185d5cdbd0c1d5a560232130df65c4345b2d3`
**CI at head:** Unified Platform Validation FAILED (npm ci lockfile out of sync, esbuild@0.28.2 missing, zod 3→4 broad) — https://github.com/egawilldoit/Ega-House-Platform/actions/runs/33128788577
**Linear parent:** EGA-529 In Review (overstates readiness), EGA-541/542 Done (stale, not final head)

This ledger is the single source for the 18 blockers; nothing is marked fixed until runtime proof exists.

| # | Blocker | Sev | Evidence | Files | Intended fix | Test required | Status |
|---|---|---|---|---|---|---|---|
| 1 | modern `createMcpHandler` auth propagation broken | P1 | `apps/web/src/lib/mcp/web-transport-handler.ts:2` uses `WebStandardStreamableHTTPServerTransport` not `createMcpHandler`; `apps/web/src/lib/mcp/create-handler.ts:1` exists but `route-runtime.ts:113` still calls `createWebMcpHandler` (legacy). Auth via `request.auth` not `handler.fetch(request,{authInfo})`. | `web-transport-handler.ts`, `create-handler.ts`, `route-runtime.ts` | Make `createMcpHandler` the real production path: per-request `McpServer` factory, `handler.fetch(request,{authInfo})` verified via SDK source https://github.com/modelcontextprotocol/typescript-sdk/blob/main/packages/server/src/server/createMcpHandler.ts, test `Bearer → verified AuthInfo → ctx.http.authInfo → tool` | `modern MCP handler` + `authenticated modern request` tests | OPEN |
| 2 | modern Host/Origin security incorrect/incomplete | P1 | `web-transport-handler.ts:51` `validateModernHeaders` returns null always, comment says transport will validate; `route-runtime.ts:118` preflight allows `Mcp-Param-*` wildcard (browsers don't). `createMcpHandler` does NOT auto-provide Host/Origin policy per SDK source. | `web-transport-handler.ts`, `route-runtime.ts` | Explicit Host/Origin validation around `/api/mcp`: expected resource host, HTTPS origin, localhost dev allowance per repo policy, bounded body, correct POST/OPTIONS/GET, CORS without wildcard, tests for good/bad Host/Origin, spoofed headers, oversized | Host/Origin boundary tests | OPEN |
| 3 | write consent radio not actually submitted | P1 | `apps/web/src/app/oauth/consent/page.tsx:108` radios rendered **outside** `<form>` (fieldset in div before form, form only has hidden `authorization_id` + deny/approve buttons). Verified in file: radios at line 108-130, form at 197. `apps/web/src/app/api/oauth/decision/route.ts:45` parses `permission_profile` but never receives it. Previous audit misclassified as P3. | `consent/page.tsx`, `decision/route.ts`, `decision-service.ts` | Put controls inside form or `form="<id>"`, prove `MCP_WRITES_ENABLED=false/true` flows, no silent upgrade, manipulated POST rejected, compensation correct | `consent form`, `grant selection` tests | OPEN |
| 4 | fake MRTR / bypassable `confirmed:true` | P1 | `apps/web/src/lib/mcp/write-tool-handlers.ts:274` `clearCompletedToday` checks `if (!input.confirmed)` and uses `mrtr_state_<hash>` not SDK `inputRequired`/`acceptedContent`, `requestState?: string` from normal args. Client can send `confirmed:true` to bypass. `apps/web/src/lib/mcp/mrtr.ts:1` is scaffolding. | `write-tool-handlers.ts`, `mrtr.ts`, `server.ts:266` | Use official SDK: `inputRequired({inputRequests:{confirm: inputRequired.elicit({message, requestedSchema})}, requestState: await codec.mint(...)})`, `ctx.mcpReq.inputResponses` + `acceptedContent`, `requestState` via `ServerOptions.requestState.verify` + `ctx.mcpReq.requestState<T>()` | MRTR tests (accept, decline, tamper, expiry, revoked, cross-instance) | OPEN |
| 5 | requestState codec not wired into server verification | P1 | `apps/web/src/lib/mcp/request-state.ts:1` `createRequestStateCodec` exists but `apps/web/src/lib/mcp/server.ts` never configures `new McpServer(...,{requestState:{verify}})` and `write-tool-handlers` never calls `verify`, uses fake prefix. | `request-state.ts`, `server.ts`, `write-tool-handlers.ts` | Configure `McpServer` with `requestState: { verify: codec.verify }`, bind `{owner, client, grantId, grantVersion, resource, tool, operationId, argsHash, target, phase, expiry}`, verify on retry + TOCTOU revalidation | requestState verification tests | OPEN |
| 6 | MCP writes bypass canonical `@ega/application` | P1 | `apps/web/src/lib/mcp/write-tool-handlers.ts:114` direct `from("projects").insert` vs `packages/application/src/projects/service.ts:28` `normalizeProjectSlug` + `repository.createProject`; `createTask` no `getScope` project/goal ownership check. `apps/web/src/lib/mcp/application-bridge.ts:1` is scaffolding. | `write-tool-handlers.ts`, `application-bridge.ts`, `packages/application/src/*/service.ts`, `packages/data-access/src/*` | For every write: `AuthInfo → McpPrincipal → AuthenticatedActor → request-scoped repository adapter → existing @ega/application use case` (projects/goals/tasks/Today/timer). Add narrow adapter if missing. Delete bridge when done. | `write handlers` + `canonical delegation` tests | OPEN |
| 7 | idempotency fail-open and not concurrency-safe | P1 | `apps/web/src/lib/mcp/write-tool-handlers.ts:60` `checkIdempotency` swallows `error` → `return {}`, `storeIdempotencyResult` swallows, `createProject` etc. not using advisory lock; `drizzle/0047` `mcp_claim_mutation_receipt` uses `ON CONFLICT DO NOTHING` without exclusive execution ownership — two callers can see `result_payload IS NULL` and both execute. | `write-tool-handlers.ts`, `drizzle/0047`, `drizzle/0048` | Redesign to fail-closed, state machine `CLAIMED/EXECUTING` + `SUCCEEDED` with atomic claimant (unique insert + `FOR UPDATE` / advisory lock), hash all material args canonical, prove 2/10 concurrent same op+same args → 1 effect, different args → conflict, ledger unavailable → no mutation | idempotency + concurrency tests | OPEN |
| 8 | exact-head CI red | P1 | `gh pr checks 183` → `Unified Platform Validation` FAILED at `242185d` (36s) `npm ci` → `package.json / package-lock.json out of sync Missing: esbuild@0.28.2` etc. Branch has local `zod 3→4` broad change (`apps/web/package.json:34` `zod ^4.4.3`) vs lockfile. | `apps/web/package.json`, `package-lock.json` | Restore deterministic install: `rm -rf node_modules; npm ci` must pass. Decide minimal zod: keep app `zod@3` and scope `zod-v4` alias to MCP schemas (per SDK guide) OR keep v4 only if fully validated. Use `npm install` not hand-edit lockfile. | `npm ci` green | OPEN |
| 9 | capability document claims more than runtime | P2 | `docs/implementation/2026-08-28-mcp-capability-coverage.md` says reads 10 / writes 18, but `apps/web/src/lib/mcp/server.ts:335` actually registers 6 reads (`ega_get_today_plan`/`ega_list_timer_sessions` were stubs `selectedCount:0`/`sessions:[]`) and 9 writes but missing archive/unarchive, health, nextStep, focus, reminders, scheduling. | `server.ts`, `capability-coverage.md`, `packages/application` | Re-audit matrix vs application ports; implement every EXPOSE or change to DEFER/EXCLUDE with product reason | capability matrix truth | OPEN |
| 10 | Today read is a stub | P2 | `apps/web/src/lib/mcp/read-tool-handlers.ts:214` `getTodayPlan` returns `{today, selectedCount:0}` not `packages/application/src/today/service.ts` `getTodayPlan` | `read-tool-handlers.ts` | Call real `getTodayPlan` via `TodayReadPort` adapter, bounded, owner-isolated, date handling | Today read tests | OPEN |
| 11 | Timer read is a stub | P2 | `read-tool-handlers.ts:230` `listTimerSessions` returns `{sessions:[], count:0}` not `packages/data-access/src/timer/repository.ts` | `read-tool-handlers.ts` | Call real `listOpenSessions`/`listRecentSessions`, bounded, open/closed | Timer read tests | OPEN |
| 12 | permission-aware discovery helper not active | P2 | `apps/web/src/lib/mcp/tool-discovery.ts:1` `filterToolsByPermissions` exists but `route-runtime.ts:136` + `web-transport-handler.ts:131` never call it; `route-runtime` always registers writes even when `!writesEnabled` (now partially fixed) but not per-principal. | `tool-discovery.ts`, `route-runtime.ts`, `web-transport-handler.ts` | Wire into per-request `McpServer` factory: `principal.permissions + writesEnabled → allowed tool set → only those registered/advertised`; `tools/list` must hide unauthorized; handlers keep permission guard defense-in-depth | discovery tests per profile | OPEN |
| 13 | OAuth/RLS authority broader than catalog | P2 | `drizzle/0046_mcp_write_rls.sql:1` includes `DELETE` under `projects.update` etc., `task_reminders FOR ALL`; `apps/web/src/lib/mcp/permissions.ts` has `delivery_runs.read` but not all delivery tools are registered; RLS vs catalog mismatch. | `permissions.ts`, `drizzle/0046`, `drizzle/0048` | Tighten RLS to least privilege: remove OAuth DELETE where not exposed (keep archive via UPDATE), split `task_reminders` by actual operation, verify `0048` split, keep application validation as primary | RLS direct tests per grant | OPEN |
| 14 | critical new modules lack focused tests | P2 | `apps/web/src/lib/mcp/create-handler.ts:1`, `audited-write-handlers.ts:1`, `mrtr.ts:1`, `tool-discovery.ts:1` etc. have no focused runtime tests; only helper existence claimed | `create-handler.ts`, `audited-write-handlers.ts`, etc. | Add tests for modern handler, Host/Origin, discovery, write delegation, MRTR, idempotency, audit, RLS, kill switch, revoked/wrong client/resource | focused tests | OPEN |
| 15 | Linear audit evidence stale/inaccurate | P2 | `EGA-529` In Review overstates readiness, `EGA-541`/`EGA-542` Done but audited `dbec2d5`/`3f3c438` not final head `242185d`; `EGA-542` says 0 P1/P2 but blockers 1-14 still open | `EGA-529`, `EGA-541`, `EGA-542` | Move `EGA-529` to In Progress with re-review comment, create fresh audit children Audit #3/#4 for final heads, preserve history via superseded comments | Linear truth | OPEN |
| 16 | PR contains unrelated planning content | P2 | `git diff --name-status origin/main...HEAD` at `242185d` includes `docs/superpowers/plans/2026-08-27-*` 7 files from `3675bab` merge (Daily Operator, Friction Radar, Health Coach, Intelligence Foundations, Smart Inbox, Weekly Review, collision map) not part of MCPV2 | `docs/superpowers/plans/2026-08-27-*` | Remove via corrective commit (not history rewrite), keep MCP plans/specs/evidence | branch hygiene | OPEN |
| 17 | architecture/docs overstate runtime | P2 | `ARCHITECTURE.md:146` claims `createMcpHandler` enabled while `route-runtime` still uses legacy transport; docs claim `full @ega/application delegation` while direct inserts remain; `requestState` HMAC verified claim while `ServerOptions.requestState.verify` not configured | `ARCHITECTURE.md`, `runbook`, `evidence` | Reconcile docs to runtime truth after fixes, remove “next increment” placeholders for claimed complete capabilities | docs truth | OPEN |
| 18 | PR description stale | P2 | PR #183 body still references old SHAs `b88213b`, `dbec2d5`, validation not at `242185d`, capability counts not at runtime, CI links old | PR #183 | Rewrite PR description at final SHA with actual SDK, serving path, auth, Host/Origin, profiles, reads/writes lists, delegation, idempotency, MRTR, RLS, migrations, validation matrix, E2E, Audit A/B, CI, P3, rollback | PR truth | OPEN |

**Next:** WAVE 1 branch hygiene, then sequential fixes per blocker, then fresh audits at exact final SHA.
