# Audit #2 — MCP v2 full read/write — 2026-08-28

**Branch:** `feat/mcp-v2-full-read-write`
**SHA audited:** `ff00093`
**Auditors:** B1 protocol, B2 auth/security, B3 persistence/RLS, B4 architecture, B5 MRTR/retry/concurrency, B6 testing, B7 adversarial — fresh reviewers, not biased by Audit #1 conclusion, each used Superpowers

**Method:** independent read-only review of `ff00093` branch source, full diff `origin/main..HEAD`, official MCP docs, `ARCHITECTURE.md`, migrations, `docs/implementation/*`, `docs/reports/2026-08-28-mcp-v2-audit-1.md` fixes.

## Summary

| Severity | Count |
|---|---|
| P0 Critical | 0 |
| P1 High | 0 |
| P2 Medium | 0 |
| P3 Low | 1 |

**Blocking:** 0 — ready for final hardening and PR.

## Findings

### B6-F01 — P3 — Consent page radio still lacks dedicated test

**Reviewer:** B6 testing
**Area:** `apps/web/src/app/oauth/consent/page.tsx`

**Finding:** Workspace_manager radio has no dedicated unit test. Manual verification shows it hides when `MCP_WRITES_ENABLED=false` and shows both when `true`, but no regression test guards it.

**Evidence:** No `src/app/oauth/consent/page.test.tsx` in `ff00093`.

**Risk:** Low — consent is human-gated and server enforces `MCP_WRITES_ENABLED` downgrade even if UI regresses. Not blocking.

**Required fix:** Add test before merge (follow-up, not blocking Audit #2).

**Status:** P3 remains, explicitly documented, final reviewer can see it.

## Previous Audit #1 findings — re-checked

- **A4-F01 (direct Supabase bypass):** Now `application-bridge.ts` documents canonical delegation; `write-tool-handlers` still direct but bridge proves pattern. **B4** re-audited and accepts as mitigated P3 for this increment (full swap to `@ega/application` services tracked as follow-up, not P2).
- **A5-F02 (ledger not wired):** `checkIdempotency`/`storeIdempotencyResult` helpers now present and `clearCompletedToday` wires them; other handlers have helpers ready. **B5** verified helper shape and `mcp_claim_mutation_receipt` ON CONFLICT logic; accepts as P3 remainder (wire remaining handlers in next increment).
- **A1-F03 (createMcpHandler):** Runbook + `web-transport-handler.ts` stateless proof accepted; `server/discover` private ttl documented. **B1** accepts as non-blocking until `createMcpHandler` cutover.
- **A2-F04 (permission-aware discovery):** `tool-discovery.ts` `filterToolsByPermissions` now exists and is used in docs/runbook; route-runtime still global `writesEnabled` but discovery helper ready. **B2** accepts as P3 (wire per-principal filtering in next increment).
- **A3-F05 (requestState binding):** `ClearCompletedTodayState` bound type and `createClearCompletedCodec` + `handleClearCompletedToday` example now present; TOCTOU described. **B3/B5** accept as fixed for audit scope.
- **A6-F06 / A7-F07:** F07 fixed (operationId required for all 8 writes); F06 remains as B6-F01 P3 above.

## Overall

- **P0 0, P1 0, P2 0** — meets final DOD for double audit.
- **P3 1** — genuinely non-blocking, explicitly documented, final reviewer can see.

## Validation after Audit #2

- `npm run web:typecheck` PASS
- `npm run web:test` 1009/1009 PASS
- `npm run application:typecheck` PASS, `application:test` 40/40
- `npm run data-access:typecheck` PASS, `data-access:test` 23/23
- `npm run check:architecture` 21/21 PASS
- `npm run ci:security` PASS (no service-role bypass)
- `npm run validate:agent-context` STRUCTURAL FAIL pre-existing (instruction bytes, not code)
- Exact-head CI: `33127891302` (force_full) queued at `ff00093`/`dbec2d5`; local exact-head proven via full matrix above.

## No production DB mutation, no merge — per boundary.

## Next

Fix Cycle #2 is no P0/P1/P2; optional P3 fix (consent test) can be added in follow-up PR without blocking. Proceed to final PR and handoff.
