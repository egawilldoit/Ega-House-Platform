# ROUTE MATRIX — EGA Command OS

Generated: 2026-08-25 · Base: dca2dce
Worktree: /home/ubuntu/ui-web · Branch: ui/web-v2

| Route | Purpose | Current structure | Server/Client | Data owner | Actions | Existing reusable components | Main UX defect | Performance risk | Accessibility risk | Refactor level |
|-------|---------|-------------------|---------------|------------|---------|------------------------------|----------------|------------------|--------------------|----------------|
| `/` | Public home / redirect | `app/page.tsx` (public) | Server | — | redirect to /dashboard or /login | none | No authenticated chrome — correct | Low | Low | KEEP |
| `/login` | Sign-in | `app/login/page.tsx` + supabase auth | Server + client form | `auth-service` | `signIn` server action | `Card`, `Input`, `Button` | Glass form with `@import` fonts, no calm focus | Low | Missing form error association | CLEAN |
| `/signup` | Sign-up | `app/signup/page.tsx` | Server + client form | `auth-service` | `signUp` | `Card`, `Input`, `Button` | Same as login | Low | Same | CLEAN |
| `/auth/*`, `/auth-ui`, `/oauth/consent` | Auth callbacks, OAuth consent | route handlers + pages | Server | supabase | oauth exchange | none | Functional, keep | Low | Low | KEEP |
| `/dashboard` | Operational command center — “what needs attention, what am I doing today, what next” | `app/dashboard/page.tsx` (async) orchestrates 8 Suspense panels; `dashboard/_lib/dashboard-data.ts`, `dashboard/_components/*` | Server (page + async panels) | `dashboard-data.ts`, `timer-service`, `focus-queue-service`, `openclaw health`, `linear` | — | `DashboardHeroSection`, `CommandCenterSpotlight`, `FocusPanelCard`, `TodayPlannerCard`, `GoalMovementCard`, `ProjectStateCard`, `ReviewPulseCard`, `TimerSummaryCard`, `PanelErrorBoundary`, skeletons | Equal-weight cards, giant hero (6.7rem), “System Healthy” badge in header, redundant Command Center key actions, overview not scannable in 2s; no Summary Strip | Duplicate fetches: page fetches timer/planner/health + panels re-fetch; `unstable_cache` on user data; no request memoization | Hero title 6.7rem fails contrast/size hierarchy; missing reduced-motion | **REDESIGN** |
| `/today` | Daily execution — “what now, what next” | `app/today/page.tsx` (363 lines) uses `getTodayPlannerData()` + cockpit panels (`StartHerePanel`, `FocusQueuePanel`, `ActiveTimerPanel`), `TodaySummaryBar`, `TodaySection`, `TodayTaskCard`, `TodaySuggestionsPanel` | Server + small client islands | `today-planner-service` | `clearCompletedFromTodayAction`, timer actions | `TodaySummaryBar`, `TodayTaskCard`, `TodayCockpitPanels`, `TimerStopOutcomePrompt` | Timeline flattened; suggestions compete with primary queue; task rows show 5-button toolbars; summary metrics not tabular | `today-planner-service` does multiple queries; realtime refresh triggers full reload | Task cards use `details/summary` without `aria-expanded` wiring | **REDESIGN** |
| `/tasks` | Work inventory — high-density list/board | `app/tasks/page.tsx` (750 lines) monolith: filters, sort, kanban, list, saved views, modals | Server (heavy JSX) | `task-service`, `task-list` lib, `task-archive`, `calendar-settings` | `archiveTaskAction`, `deleteTaskAction`, `pinTaskAction`, `updateTaskInlineAction`, `startTimerAction` | `TaskFilterControls`, `TaskKanbanCard`, `TasksWorkspaceShell`, `InlineTaskUpdateForm`, `FocusPinToggleForm`, `TaskSavedViewsPanel` | Dense but not scannable: status/title/project/priority/due/estimate all competing; filter pills pill-everything; kanban drag affordance weak; page.tsx 750 lines | Single `getTasksWorkspaceData` is efficient, but 750-line JSX blocks streaming; no virtualization at scale | Filter pills missing `aria-pressed`; kanban no keyboard reorder | **RESTRUCTURE** |
| `/tasks/projects` | Project index | `app/tasks/projects/page.tsx` | Server | `project-archive`, supabase `projects` | — | `Card`, `StatusBadge` | Health/progress/due visible but Owner column redundant | Low | Color-only health | CLEAN |
| `/tasks/projects/new` | Create project | `app/tasks/projects/new/page.tsx` | Server + client form | `task-service` | create project | `Input`, `Textarea`, `Button` | Calm but not keyboard-optimized | Low | OK | CLEAN |
| `/tasks/projects/[slug]` | Project detail | `app/tasks/projects/[slug]/page.tsx` | Server | `task-service` + `projects` | update, archive | `Card`, `EmptyState`, task list | Linked goal + tasks not visually prioritized | Extra fetch for tasks by project | Missing landmark for detail sections | CLEAN |
| `/goals` | Goals list — “direction” | `app/goals/page.tsx` (558 lines) | Server + inline forms | `@ega/application` + `SupabaseGoalsRepository` | `updateGoalHealthAction`, `updateGoalNextStepAction`, `archiveGoalAction` | `InlineGoalHealthForm`, `InlineGoalNextStepForm`, `Card` | Objective/progress/tasks mixed; health/status inline forms clutter; no primary-job hierarchy | `getGoalsReadModel` + supabase tasks count N+1 for progress | Inline forms lack `aria-live` for health change | **REDESIGN** |
| `/timer` | Focus session — “active session is primary” | `app/timer/page.tsx` (506 lines) + `ActiveTimerDisplay` (client tick), `TimerStopForm`, `SessionTimingEditor` | Server + 1s client tick island | `timer-service` | `startTimerAction`, `resolveSessionConflictAction`, `updateSessionTimingAction` | `ActiveTimerDisplay`, `TimerStopForm`, `TimerActionFeedback` | Active session competes with breakdown/recent; elapsed/tabular-nums not isolated; 1s tick risk if parent re-renders | Tick isolated correctly today, but page re-fetches `getTimerWorkspaceData` per nav | Live region for elapsed not `aria-live=polite` | **REDESIGN** |
| `/review` | Weekly review — “what happened, what changes” | `app/review/page.tsx` (506 lines) + `SessionHeatmap`, `WeekBarChart`, `ReviewForm` | Server + client charts | `weekly-review-page-service`, `week_reviews` | `ReviewForm` submit, email preview | `SessionHeatmap`, `WeekBarChart`, `StatPanel`, `WeekSelector` | Review status/completion/trends/reflection cramped; charts lack titles/units; no insight wiring | Charts eagerly loaded, not dynamically imported | Canvas/charts missing text alternative | **REDESIGN** |
| `/review/[reviewId]` | Review detail | `app/review/[reviewId]/page.tsx` | Server | `weekly-review-page-service` | — | `Card` | Secondary | Low | OK | CLEAN |
| `/work-analytics` | Operational evidence — “Focused time +24% vs last week” | `app/work-analytics/page.tsx` (326 lines) + `InteractiveAnalytics`, `AnalyticsFilters` | Server + client charts | `work-analytics-data-adapter`, `work-analytics-report-builder` | — | `Card`, `InteractiveAnalytics` | Sparklines without explicit question/title; filter not URL-backed fully | `getWorkAnalyticsSessionsForWindow` per range, not cached; charts not lazy | Missing `table` fallback for charts | **REDESIGN** |
| `/ideas` | Capture — frictionless | `app/ideas/page.tsx` (393 lines) | Server + client forms | `idea-note-service` | `CreateIdeaNoteForm`, `EditIdeaNoteForm`, archive | `CreateIdeaNoteForm`, `IdeaNoteArchiveControls`, `Input`, `Badge` | Triage vs capture vs conversion not separated; filter bar dense | Full list query per filter, no pagination proof | Search input label OK | CLEAN |
| `/startup` | Startup ritual — intentional daily ritual | `app/startup/page.tsx` (78 lines) → `StartupPlanner` component | Server | `startup-planner-service` | — | `StartupPlanner`, `Card` | Currently thin wrapper — needs hierarchy: priorities/attention/focus decision; currently clone-ish of dashboard | One service call, fine | OK | **REDESIGN** |
| `/shutdown` | Shutdown ritual — completion/carry-over | `app/shutdown/page.tsx` (181 lines) | Server + forms | `shutdown-service` | `carryForwardTaskToTomorrowAction`, `saveShutdownReflectionNoteAction` | `ShutdownReflectionForm`, `ShutdownTaskList`, `Badge`, `Card` | Similar to Today but not distinctive; reflection buried | Two queries for today/tomorrow | OK | CLEAN |
| `/apps` | Launcher — “actual integration state” | `app/apps/page.tsx` + `AppsLauncherGrid`, `APPS_LAUNCHER_ITEMS` | Server | static meta | — | `AppsLauncherGrid` | Grid is calm but not densified; “Apps” label duplicates TopBar | Low | Keyboard nav verified (grid focus) | CLEAN |
| `/apps/[workspace]` | Workspace embed | `app/apps/[workspace]/page.tsx` | Server | — | — | — | Verify URL param handling | Low | — | KEEP |
| `/help` | Help — search/categories/shortcuts | `app/help/page.tsx` (147 lines) | Server | static | — | `Card`, `Badge` | Categories/shortcuts present but no search (static FAQ) | Low | OK | CLEAN |
| `/settings/account` | Account — stable nav, clear forms | `app/settings/account/page.tsx` (171 lines) | Server | `calendar-settings-service` | `updateCalendarDefaultsAction`, `disconnectGoogleCalendarAction` | `Card`, `Input`, `Button` | Forms OK; destructive action separation needs polish | Low | Form labels OK | CLEAN |
| `/search` | CommandPalette backend | `app/search/actions.ts` | Server action | `workspace-search-service` | `searchWorkspaceAction` | — | Correct | Debounce OK but no abort | — | CLEAN |

**Refactor legend**: KEEP = no structural change; CLEAN = token/a11y/perf polish in place; REDESIGN = visual recomposition required; RESTRUCTURE = file ownership / page.tsx thinning + new `_components`/`_lib` needed.

**Mobile isolation**: `git diff --name-only origin/main...HEAD | grep '^apps/mobile/'` → (expected no output; verified in Wave 0 commit).
